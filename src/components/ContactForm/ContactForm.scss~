.contact-form-container {
  display: flex;
  justify-content: space-between;
  gap: 2rem;

  @media (max-width: 1024px) {
    flex-direction: column;
  }

  @media (min-width: 1024px) {
    flex-direction: row;
    width: 100%;
  }
}

.contact-form, .contact-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.contact-form {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.contact-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  justify-content: space-between;
  gap: 2rem;
}

.contact-info h2 {
  color: var(--highlight-color);
}

.social-icons {
  display: none; /* Hide by default */

  @media (max-width: 768px) {
    display: flex; /* Show on mobile when side menu is not visible */
    align-items: center; /* Center horizontally */
    justify-content: center; /* Center vertically */
    gap: 3rem;
    font-size: 2rem;
    position: relative; /* Required for pseudo-elements */

    &::before {
      content: "";
      position: absolute;
      top: -30px; /* Move the glow effect slightly higher */
      left: -10px; /* Move the glow effect slightly to the left */
      right: -10px; /* Move the glow effect slightly to the right */
      bottom: -10px; /* Move the glow effect slightly to the bottom */
      border-radius: 20%;
      background: linear-gradient(45deg, var(--i), var(--j));
      filter: blur(20px); /* Increase the blur to make the glow effect wider */
      z-index: -1;
      opacity: 1; /* Ensure the glow effect is always visible */
      transition: opacity 0.5s;
    }

    [data-theme='dark'] &::before {
      background: rgba(102, 204, 255, 0.3); /* Dark mode glow color */
    }

    [data-theme='light'] &::before {
      background: rgba(155, 89, 255, 0.5); /* Light mode glow color */
    }
  }
}

.phone-number {
  font-size: 1.2rem;
  color: var(--text-color);
}

.contact-info-title {
  font-size: 2rem;
  margin-bottom: 1rem;
}

.contact-form {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.contact-form.light {
  color: var(--dark-gray);
}

.contact-form.dark {
  color: var(--grid-white);
}

.form-input,
.form-textarea {
  padding: 0.75rem;
  border: 1px solid var(--blog-card-border);
  border-radius: 0.5rem;
}

.form-input.light,
.form-textarea.light {
  background: var(--foreground-dark);
  border-color: var(--border-card);
  color: var(--dark-gray);
}

.form-input.dark,
.form-textarea.dark {
  background: var(--card-bacground-dark);
  border-color: var(--border-card-dark);
  color: var(--grid-white);
}

.form-button {
  padding: 0.75rem 1.5rem;
  background: var(--highlight-color);
  color: white;
  border: none;
  border-radius: 0.5rem;
  cursor: pointer;
}

.form-button.light {
  background: var(--text-color);
  color: var(--grid-white);
}

.form-button.light:hover {
  background: var(--highlight-color);
}

.form-button.dark {
  background: var(--highlight-color);
  color: var(--grid-white);
}

.form-button.dark:hover {
  background: var(--text-color);
}
